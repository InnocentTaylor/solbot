kind = "web"
previewPath = "/"
title = "Solana Meme Bot"
version = "1.0.0"
id = "artifacts/sol-bot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19480

[services.development]
run = "pnpm --filter @workspace/sol-bot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sol-bot", "run", "build" ]
publicDir = "artifacts/sol-bot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19480"
BASE_PATH = "/"
